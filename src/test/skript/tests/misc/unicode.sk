test "unicode":
	assert formatted "<unicode:00A7>" is "§" with "single symbol did not get replaced"
	assert formatted "<u:00A7>" is "§" with "single short symbol did not get replaced"

	assert formatted "a<unicode:1F41B>B" is "a🐛B" with "symbol did not get replaced"
	assert formatted "a<u:1F41B>B" is "a🐛B" with "short symbol did not get replaced"
